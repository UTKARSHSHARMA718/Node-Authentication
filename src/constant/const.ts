export enum EApplicationEnvironment {
    PRODUCTION = "production",
    DEVELOPMENT = "development",
}

export const VALID_IMAGE_TYPES = ["image/jpeg", "image/png"]

export const VALID_EMAIL_VERIFICATION_TIME_IN_MINUTES = 15

export enum EROLE {
    ADMIN = "admin",
    USER = "user",
}
